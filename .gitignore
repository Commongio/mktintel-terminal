# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*

# Original (unoptimized) theme videos — kept locally, never committed.
# The web-optimized versions in public/themes/ are the ones that ship.
media-source/

# Stray Obsidian vault artifacts that landed in the repo root — not part of the app.
.obsidian/
*.canvas
2026-*.md

# The one .env the repo commits: a template with no values in it. Without this
# negation the `.env*` rules above would hide it and every new checkout would
# have to guess which variables exist.
!.env.example
